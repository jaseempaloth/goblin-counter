# Icon Assets

The extension logo is kept as an editable SVG source and exported to the PNG
sizes Chrome expects.

- `goblin-counter-logo.svg` is the master artwork.
- `icon16.png`, `icon48.png`, and `icon128.png` are referenced by
  `manifest.json`.

If the SVG changes, export fresh PNGs before loading the extension in Chrome.
